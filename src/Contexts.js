import { createContext } from "react"

// width for first cylinder (r1)
export const WidthContext = createContext(125)
export const MaterialContext = createContext("acero")
export const TemperatureContext = createContext(25)